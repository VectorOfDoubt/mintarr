services:
  tests:
    build:
      context: .
      dockerfile: Dockerfile.test
    image: mintarr-tests:local
    environment:
      - TIDALHIRES_API_KEY=tidalhires-test-api-key
      - DOWNLOAD_BASE=/tmp/downloads
      - OUTPUT_BASE=/tmp/output
      - TIDAL_DL_NG_CONFIG=/tmp/tidal-config
      - MINTARR_REPO_ROOT=/repo
    volumes:
      - type: bind
        source: ./app
        target: /app
      - type: bind
        source: ./tests
        target: /tests
      - type: bind
        source: .
        target: /repo
        read_only: true
    working_dir: /app
    command: ["pytest", "/tests", "-v", "--tb=short"]

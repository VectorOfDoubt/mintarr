# Mintarr runtime dependencies, mirrored from the Dockerfile.
# The Dockerfile is authoritative; this file exists so CI jobs (mypy,
# tooling) and contributors can pip install without building the image.
flask==3.1.3
gunicorn==26.0.0
requests==2.32.5
# tidal-dl-ng (Radexito fork) — pinned to the same SHA as the Dockerfile.
tidal-dl-ng @ git+https://github.com/Radexito/tidal-dl-ng-For-DJ.git@87ec210dfeeef23441b7c99a16123a25ec63f207

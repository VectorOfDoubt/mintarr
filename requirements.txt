# Mintarr runtime dependencies, mirrored from the Dockerfile.
# The Dockerfile is authoritative; this file exists so CI jobs (mypy,
# tooling) and contributors can pip install without building the image.
#
# tidal-dl-ng (Radexito fork) is intentionally not listed here: pip
# rejects the egg-name mismatch and the tool is only needed at runtime
# (subprocess call from app/adapters/tidal.py). The Dockerfile installs
# it directly. mypy + ruff do not need it; missing-import warnings are
# tolerated via mypy config.
apprise==1.9.0
flask==3.1.3
gunicorn==26.0.0
mutagen==1.47.0
prometheus-client==0.21.1
requests==2.32.5
